# 历史资料

这里保存 monorepo 合并前的交接文档和双仓运行说明，仅用于追溯决策。

- `backend-handoff/`：早期前后端 V16 对接与设备迁移记录。
- `pre-monorepo/`：后端仍以独立 checkout 管理时的版本指针和 Docker 说明。

这些文档中的目录、仓库地址和启动命令可能已经失效。当前用法以仓库根 README、`backend/README.md` 和 `backend/deploy/README.md` 为准。
