# 主仓约定

- 项目名：`swufe-rag`
- 主仓地址：`https://github.com/ZorIgn/swufe-rag`
- 默认分支：`main`
- 本仓库是团队集成基线，其他模块通过分支和 Pull Request 接入。
- 禁止向 `main` 强制推送，禁止用无关历史合并覆盖他人工作。
- 模拟知识块只存在于 `tests/fixtures/`，ID 全部以 `fixture_` 开头。
- 索引、向量、模型缓存、密钥和评估输出不提交 Git。

